{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug thermostat_app with default args",
            "program": "${workspaceFolder}/target/debug/thermostat_app",
            "args": [
                "--config", "${workspaceFolder}/thermostat_app/resources/config.json",
                "${workspaceFolder}/thermostat_app/resources/test.gcode"
            ],
            "cwd": "${workspaceFolder}",
            "sourceLanguages": ["rust"]
        }
    ]
}