[package]
name = "thermostat_service_client"
version = "0.1.0"
edition = "2024"

[dependencies]
serde_json = "*"
tonic = "0.13.1"
prost = "*"

ipc = { path = "../../ipc" }
client = { path = "../../client" }
uart_sized_package_reader_writer = { path = "../../ipcs/uart_sized_package_reader_writer" }
thermo_data = { path = "../../data/thermo_data" }

[dev-dependencies]
mockall = "*"

[build-dependencies]
tonic-build = "0.13.1"